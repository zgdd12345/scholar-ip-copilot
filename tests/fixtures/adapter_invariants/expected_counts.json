{
  "_note": "Two kinds of state live in this file. (1) `commands` / `agents` / `skills` / `hooks` / `templates_root_dirs` / `retention_commands` — DERIVABLE from the source tree. They are intentional drift catchers: bumping them is a deliberate PR-visible signal. Tests in tests/test_adapter_conformance.py re-count the source tree and assert equality. (2) `_mcp_legacy_baseline` — GENUINE ratchet state (not re-derivable). See its own inline `_note` for the ratchet semantics.",
  "commands": 20,
  "agents": 15,
  "skills": 24,
  "hooks": 7,
  "templates_root_dirs": 2,
  "retention_commands": ["polish", "xreview"],
  "_mcp_legacy_baseline": {
    "_note": "Files in rendered adapter output that still mention legacy MCP server names (scholar-search-mcp, bib-manager-mcp, latex-build-mcp, code-intel-mcp, experiment-mcp, patent-search-mcp, external-agent-mcp). Invariant D is implemented as a ratchet: this set must not GROW. After the 2026-05 cleanup the baseline is empty; if a future change reintroduces a *-mcp reference the test will fail and force a deliberate add.",
    "claude_code": [],
    "codex_cli": []
  }
}

# Experiments to validate

Populate via `workflow:paper.idea`. One row per experiment that must be run to back up a
claim in the novelty matrix.

| Experiment | Validates Idea | Hypothesis | Setup | Metric(s) | Expected Outcome | Status | Evidence ids |
|------------|----------------|------------|-------|-----------|------------------|--------|--------------|

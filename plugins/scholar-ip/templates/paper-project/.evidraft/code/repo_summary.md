# Repository summary

Populate via `/scholar:paper-code-audit` (or refreshed by hand). Keep this skeleton's
section headers stable — downstream commands read by heading.

## Languages

- TODO: list primary languages and approximate share (e.g. Python 80%, C++ 15%).

## Top-level modules

- TODO: list top-level packages / directories and a one-line purpose.

## Entry points

- TODO: list executable scripts, `__main__`s, CLI entry points, server entries.

## Configs

- TODO: list config files (`pyproject.toml`, `package.json`, YAML/JSON configs,
  `Makefile`, `Dockerfile`) and what each controls.

## Tests

- TODO: list test directories, test runner command, and current coverage if
  known.

## Build / run

- TODO: minimal command(s) to install dependencies, build, and run the project.

## Open questions

- TODO: anything unclear after a first pass. Mark unknowns explicitly — do not
  guess.

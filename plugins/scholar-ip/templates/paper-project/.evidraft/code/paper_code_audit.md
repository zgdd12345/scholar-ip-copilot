# Paper-vs-code audit

Populate via `/scholar:paper-code-audit`. One row per paper claim that the code is
expected to support. `Verdict` must be one of:
`CONFIRMED` / `PARTIAL` / `MISSING` / `MISMATCH` / `NOT_AUDITABLE`.

| Paper claim | Where in paper | Code evidence (file:lines) | Verdict | Notes |
|-------------|----------------|----------------------------|---------|-------|

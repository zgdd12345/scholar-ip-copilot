# Claim chart

Populate via `workflow:patent.prior-art` (skeleton) and refine via `workflow:patent.claims`.
One row per claim element. `Risk` is one of `low` / `medium` / `high`.

| Claim element | Specification support | Code support | Prior art overlap | Risk | Suggested revision |
|---------------|-----------------------|--------------|-------------------|------|--------------------|

"""EviDraft adapter packages.

Each subpackage renders the platform-neutral plugin source at
``plugins/scholar-ip/`` into a host-specific layout (Claude Code, Codex CLI,
OpenCode). Shared YAML/frontmatter loading lives in ``_shared.loader``.
"""

__version__ = "0.0.1"
